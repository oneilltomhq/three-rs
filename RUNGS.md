# Rung log

| rung | example | result | commit on `port` | notes |
|---|---|---|---|---|
| 0 | grader | calibrated | bdbb76a (handoff) | see rung0/RUNG0.md |
| 1 | webgpu_depth_texture | PASS 0/100000 px, Intel Iris Xe | 2362510 | max RGB distance 15.6 of 44 limit; hand-written WGSL in src/renderer/shaders/{basic,quad}.wgsl to replace at rung 4; in-page devicePixelRatio is 1 (grader sets no deviceScaleFactor) |
| 2 | webgpu_instance_mesh | PASS 60/100000 px since rung 9 (was 45 with clear alpha 1; three itself scores 60 vs the same JPEG) | f15e158 | key finding: scene renders into an internal rgba16float MSAA target in linear, then a separate output pass applies sRGB (Renderer.needsFrameBufferTarget / _renderOutput); converting inside the scene pass fails at edges. New WGSL: normal_world_range_mix.wgsl, output_color_transform.wgsl |
| 3 | webgpu_materials_basic | PASS 0/100000 px (max RGB distance 43.0 of 44 limit, single JPEG-ringing pixels on edges) | f4c47da | cube texture rgba8unorm-srgb (GPU does sRGB→linear, no colour-space node), Three's mipmap blit is per-face 2d-array bilinear box, background is a BackSide SphereGeometry(1,32,32) drawn first with depthCompare always. New WGSL: basic_envmap, background_cube, mipmap. Last rung with hand-written WGSL. |
| 4 | webgpu_rtt | PASS 1/100000 px (one silhouette pixel on the box) | b597b20 | Ported node system (docs/nodes.md): NodeBuilder emits both WGSL stages + bindings, renderer names no material. Generated WGSL structurally identical to Three's dumps; divergences listed in docs/nodes.md §7. All six hand-written WGSL files deleted; only mipmap.wgsl remains (Three's own is raw WGSL). Rungs 1–3 unchanged at 0/45/0. Residue: zune-jpeg vs libjpeg-turbo decode differs by ≤3 per channel on every JPEG texture. Gaps: groups discarded, ClampToEdge only, no transparent list, needs_frame_buffer_target hardcoded. |
| 5 | webgpu_lights_phong | green on branch `rung5` at 0e197ec (from port 7a3a30f merge, pre-treewalk); verified by the director 2026-09-13 00:55: e2e rerun serially 5/5, 4 of 100000 px, strip looked at (three teapots + light sphere match) | 0.004% | WGSL structurally identical to Three's four programs (docs/nodes.md §7 sub-build layers: SUB_BUILD + NORMAL_VALUE thread-locals, normal_view memoised on (layer, value); §8 cosmetic deltas). Two pixel bugs found only by the grader: Object3D.rotation.y written directly did not sync the quaternion (teapots side-on, 2.41%); Phong specular default was raw 0x11/255 instead of Color(0x111111) sRGB→linear 0.0056 (centre teapot hotspot, 0.175%). Ruled out: zune vs libjpeg decode (max channel delta 3), mipmap shader, texture colour spaces. Merged onto port at 7ca38ca (2026-09-13 00:39): 31 px there, the extra pixels are teapot silhouette edges from the rung 9 clear alpha 0 (Three scores 0.0%). |
| 6 | webgpu_morphtargets | PASS 0/100000 px, verified by the director 2026-09-13 01:45 (serial rerun 7/7, strip looked at: flat red box on blue matches) | merged into port (see below) | Branch rung6 389256c: PerspectiveCamera owns a Node (scene.add(camera), light parented to camera); AmbientLight + sortLights(); flatShading via normalFlat (no normal attribute); morph targets: getEntry() 4096×2 rgba32float 2d-array texture, morphReference(), Loop/If statement nodes, ivec2, layered textureLoad, vertex-only influences buffer, base uniform. Vertex WGSL matches statement-for-statement incl. the morph loop; §8 gained three cosmetic deltas. Bug found only by the multi-material dump: normalView memo key ignored flatShading. Graded frame has both influences at 0, so the loop is verified off-path (MORPH_INFLUENCES env hook + tests/nodes_morph.rs). Not ported: morphAttributes.normal/.color. |
| 13 | webgpu_tsl_galaxy | PASS 40/100000 px, verified by the director 2026-09-13 01:50 (serial rerun 8/8, strip looked at: spiral galaxy matches, residue is scattered single sprites) | merged into port at ac00b8a (see below) | Branch rung13 e67972f (from c1933b9): SpriteNodeMaterial (MaterialKind::Sprite; position/scale/rotation nodes, size_attenuation) via the setup_position_view seam — position_view()/model_view_projection() are now one context-keyed pair like normal_view(), behaviour-preserving with no override; BufferSource::Range carries [f64;4] ends built by tsl::RangeValue (Three's three widening rules; vec3 range has w=0) and instanced_range() applies the narrowing convert(); TSL two_pi/rotate(vec2)/pow3/.zw()/material_rotation; InstancedMesh::new fills identity matrices (was zeros). Determinism: Renderer::skip_random_draws(5) for the Inspector List ids, fill order branchAngle→radiusRatio→randomOffset→scaleNode pinned by tests/nodes_range_buffers.rs against Three's uploaded buffers. WGSL matches statement-for-statement incl. uniform offsets (materialOpacity @28). docs/nodes.md §9.3, §10. Not done: RotateNode vec3/vec4, Sprite object center, alphaToCoverage/colorWrite. |
| 7 | webgpu_shadowmap | PASS 7/100000 px, verified by the director 2026-09-13 01:55 (serial rerun on the branch and after the merge, strip looked at: spot + directional shadows on the noise ground match, residue on shadow edges) | merged into port at d71c065 (see below) | Branch rung7 84316f8 (from 7ca38ca): LightObject with LightKind::{Ambient,Point,Spot,Directional}, LightShadow + ShadowCamera (Perspective/Orthographic), Renderer.shadow_map_enabled, cast_shadow/receive_shadow, render_shadows() before material setup (2D maps only, PCF via textureSampleCompare; no point-light cube shadows), shadow_material() per object (NoBlending, flipped side), MaterialX mx_fractal_noise (tests/nodes_mx_noise.rs), positionLocal as a varying, mask_node → discard, ToneMapping::AcesFilmic in render_output, Background::Node. All eight programs diffed against the r186 dumps; four new cosmetic divergence classes in docs/nodes.md §8 (MaterialX fn order, splatted constants, re-evaluated shared sub-expressions, fog literals). Design notes in docs/rung7-progress.md: shadow factor multiplies light colour before cone/distance attenuation; five-condition frustum guard with else 1.0; frontFace cw falls out of _shadowSide. |
| 8 | webgpu_lights_physical | PASS 4/100000 px, verified by the director 2026-09-13 02:30 (serial rerun on the branch and after the merge 10/10, strip looked at: bulb over the hardwood floor, cube/ball contact shadows, hemisphere fill match) | merged into port (see below) | Branch rung8 (c7d6907, 10 commits from 7ca38ca): PhysicalLightingModel (src/materials/physical.rs: BRDF_GGX, DFG LUT, multiscattering), MeshStandardNodeMaterial/MeshPhysicalNodeMaterial aliases + PBR fields, HemisphereLight, PointLight cube shadows (CubeDepthTexture, six-face pass, PointShadowNode + Vogel filter through textureSampleCompare), BumpMapNode + front_facing, Texture offset/repeat uv matrix, Reinhard + toneMappingExposure. Worker findings (docs/rung8-progress.md): normalWorld/tangent frame were singletons ignoring the material normal (now keyed like normalView, docs/nodes.md §7); needs_var missed Swizzle/Neg (explicit to_var); comparison sampler needs sampler_comparison in WGSL + Comparison binding + Depth sample type. |
| 9 | webgpu_postprocessing_masking | PASS 18/100000 px, verified by the director 2026-09-13 00:20 (serial rerun, strip looked at: torus + box masks over the two textures match) | cc77468 | PassNode (src/renderer/pass.rs) renders a scene into an rgba16float target; RenderPipeline (src/renderer/render_pipeline.rs) composes with mix on the mask alpha; docs/postprocessing.md. Renderer clear alpha is now 0 like WebGPURenderer alpha:true, which moved rung 2 45 -> 60 (Three's own score) and rung 5 4 -> 31. App calls PassNode::render before RenderPipeline::render. |

## Side branches (cut from f4c47da / 7c15ea5; merged onto `port` after rung 4)

| branch | state | notes |
|---|---|---|
| viewer | merged 4e3cf37; done, verified (window opens on Wayland, orbit/zoom/pan correct, present is byte-identical to canvas) | `cargo run --release --bin viewer -- <example>`; renderer seam is `src/renderer/present.rs` + 4 small hunks in mod.rs |
| geometries | merged e637e65 (geometries moved onto the attribute map); done, 29 tests, all bit-exact vs three.js samples | Box, Plane, Cylinder, Cone, Torus, Polyhedron family, Circle, Ring, Lathe, Capsule, Teapot addon, RoundedBox addon, to_non_indexed. `*_with_groups()` variants until BufferGeometry gets `groups`; math extras in src/geometries/math_extras.rs to fold into src/math |
| unit-tests | merged 63b717f; done, verified: 293 unit tests + 3 e2e green (7c15ea5, 13 commits) | Three's QUnit math/core tests: Vector2/3/4, Matrix3/4, Quaternion, Euler, Color, MathUtils, Object3D transforms, BufferAttribute/Geometry, PerspectiveCamera view/film API. Two fixes: compute_vertex_normals panicked on a trailing partial triangle; Color::default() now white like Three. No graded image moved. Skipped: no-parent Object3D tree ops, named-attribute map ops, JSON/uuid/typed-array families. |
| math2 | merged (clean); done, verified: 462 tests + 3 e2e green (826cc9f, 14 commits) | Box2, Box3, Sphere, Plane, Ray, Frustum, Triangle, Line3, Spherical, Cylindrical, Matrix2, SphericalHarmonics3, ColorManagement with Three's tests (166 new). Skipped: Box3/Frustum object-tree cases (need children + geometry bounds), Triangle getInterpolatedAttribute. Notes: ColorManagement ported as a value type; Three's numEqual is 0.1 not eps; several vacuous Three test assertions reproduced verbatim. |
| scene-graph | merged 7d03ee0; done, verified: 330 unit + 3 e2e green (0899b56) | Node = Rc<RefCell<Object3D>> with Weak parent, Object3DNode trait for tree ops (docs/scene-graph.md); Group; BufferGeometry named attribute map, groups, drawRange, morphAttributes, to_non_indexed, transforms. Renderer still walks the flat Child list; folding Child into the tree is left for the first rung that nests. Fixed: applyMatrix4/updateMatrixWorld now honour matrixAutoUpdate / matrixWorldNeedsUpdate. |
| animation | merged; done, verified: 409 tests green on branch (3902bc7, 7 commits) | Interpolant family, KeyframeTrack + six typed tracks, AnimationUtils, AnimationClip (parse/toJSON/subclip/makeClipAdditive), PropertyMixer, AnimationAction, AnimationMixer (arena, ActionHandle), AnimationObjectGroup. PropertyBinding: parsing complete; getter/setter half deferred behind `BindingTarget`/`TargetResolver` traits in src/animation/binding_target.rs, which Object3D implements at rung 10. 113 new tests. Skipped: Bezier interpolant, loop/finished events, clipAction by name (needs root.animations). |
| gltf | merged onto port; done, verified: 7 loader tests, 646 total, e2e 0/45/0/1 (c351a38, 5 commits) | Rung 10 data side: GLTFLoader (GLB, .gltf, data URIs, all accessor types incl. sparse/interleaved, primitives → BufferGeometry, nodes → Object3D tree, skins → Skeleton, animations → AnimationClip; materials/textures as data records), SkinnedMesh/Skeleton/Bone, SceneResolver + NodeTarget binding position/quaternion/scale/morphTargetInfluences. Expected numbers from Three's own loader (tests/gltf/samples/sample.mjs). Bug caught: bind must use the identity matrix, not the inverse world matrix. Note: webgpu_skinning loads Michelle.glb and plays SambaDance. Remaining: CUBICSPLINE, image decode from bytes, geometry dedup/groups, extensions. |
| d3-layout | merged; done, verified: 100 tests green (8ea9450, 8 commits) | Workspace crate d3/hierarchy (d3-hierarchy v3.1.2): hierarchy/Node, cluster, tree, partition, pack, treemap (all tilings), stratify. Arena Tree with index parent/children, data as serde_json::Value (README). All values bit-identical to the JS on first run. Note: 3.1.2 ships no tape tests for cluster/tree/partition; those expectations were generated by running the vendor JS. Skipped: flare-test (needs d3-dsv), Map/Set inputs, fuzzers. |

After the merges `port` is at the animation merge commit: 635 tests + 4 e2e (0 / 45 / 0 / 1). The range() per-render random draw flagged by the viewer worker is gone: rung 4 caches the range buffer per renderer; the viewer's fresh-renderer-per-time workaround is now redundant but harmless. Still to fold: geometries' `*_with_groups()` tuples into BufferGeometry.groups, math_extras into src/math, geometries' to_non_indexed into BufferGeometry::to_non_indexed. Note: running e2e concurrently from two worktrees on the one GPU produced one spurious failure; run e2e serially.

## Scout plans (handoff/scouts/rungN/PLAN.md, with Three's dumped WGSL and the reference image)

Rungs 6, 7, 8, 9 scouted 2026-09-12 23:20–23:31. Picks made: rung 8 = webgpu_lights_physical, rung 9 = webgpu_postprocessing_masking (see HANDOFF ladder). Common thread: rungs 6, 7 and 8 all nest objects (camera→light, Group→light, light→bulb); rung 5's Scene::drawables() covers light children only, so the first of these to land should make the renderer walk the real Node tree.

## Re-pin to r186 (done 2026-09-13 00:05)

Vendor three.js moved 3d010ef → 148ef33 (tag r186); grader-flags.patch applied unchanged. Rung 0 recalibrated on all nine ladder examples plus textures_2d-array: every number identical to 3d010ef (instance_mesh still 0.1% pass). No ladder reference JPEG changed; port e2e 0/45/0/1 unchanged. Scout WGSL for rungs 6–9 re-dumped (`-r186` suffix): byte-identical except the banner and one renamed NodeBuffer struct in rung 6. Chrome moved .54 → .75 under puppeteer-core 25.10.0 with no visible effect. Details: rung0/REPIN-r186.md. d33 should bump its pin to 148ef33 too.

## sdf-text scout (handoff/scouts/sdf-text/PLAN.md, 2026-09-13)

Two headline findings: three r186's NodeMaterial.setupPosition applies positionNode *after* the instance transform, which is the bug d33 worked around by baking glyph quads into instance matrices; the port implements position_node correctly (before the instance matrix) and still pixel-matches because the two are identical on a unit plane. The EDT binarises at alpha >= 128, so the Canvas-AA question shrinks to "which texels are >= 50% covered". Ladder: 1 font metrics/outlines vs opentype.js goldens, 2 raster+EDT+atlas tiles, 3 layout_text_vector + six lib3 layers, 4 renderer capabilities (instanced vertex attributes, R32Float + FLOAT32_FILTERABLE, fwidth/abs, position_node, per-fragment alpha, blending, transparent list), 5 BatchedText block, 6 d33 treemap labels. Steps 1–3 need no GPU; step 4's blending/transparent list is shared with rung 9.

## Tree walk merged (2026-09-13)

Branch treewalk (12a8623) merged into port (cdf834a) after foldins. `Child` is gone: Mesh/InstancedMesh are `Payload` variants on Object3D, `Mesh::new` returns a `Node`, Scene owns a root Node, `Renderer::project_scene` builds a RenderList (opaque/transparent/lights, visible/layers/groupOrder/frustumCulled, painterSortStable) in src/renderer/render_list.rs. Object3D gained layers/render_order/frustum_culled (Layers.tests.js ported). Pixels unchanged (0/45/0/1). Workspace: 767 tests, 54 targets, e2e 4/4. Merge notes for rung5 are in the treewalk worker's report: Scene.lights/drawables() are obsolete, lights become Payload::Light collected by project_object in traversal order. SkinnedMesh still owns its own Node (rung 10 step).

## Rung 9 and rung 5 on port (2026-09-13 00:39)

port: cdf834a (treewalk) -> cc77468 (rung 9, fast-forward) -> 7ca38ca (merge rung5). Workspace 769 tests; serial e2e 6/6: depth_texture 0, instance_mesh 60, lights_phong 31, materials_basic 0, postprocessing_masking 18, rtt 1. Rungs 6, 7, 8 and the shared blend/instanced-attribute branch start from 7ca38ca.

Scouts rung 11 (webgpu_mesh_batch: no storage buffers, no indirect draws, 453 plain drawIndexed calls + three DataTextures; five Inspector Math.random draws precede the instance loop) and rung 12 (webgpu_compute_points: graded image is 4 pixels and cannot distinguish a working compute stage from a black frame; gates are WGSL diffs + storage readback; companion image gate proposed: webgpu_compute_texture) are in scouts/rung11 and scouts/rung12.

## sdf-text steps 0–3 merged (2026-09-13 00:44)

Branch sdf-text (4e1d30f, 4 commits) merged into port at 390dcfd. Workspace crate `sdf-text` (ttf-parser outlines, exact-area analytic rasteriser in src/raster.rs — tiny-skia dropped, Felzenszwalb EDT in f64, glyph atlas, layoutText/layoutTextVector, Text member). 52 tests, no GPU, all against goldens dumped from the lib3 JS: metrics/advances/bboxes/paths/kern pairs exact, EDT bit-exact, atlas placement exact, layouts bit-exact. Two bounded epsilons vs Chromium's canvas raster (Skia fixed-point edge quantisation, measured not guessed): 52 of 65536 mask pixels flip for 'a', tiles differ by at most 2/64, axis-aligned glyphs bit-exact. Two lib3 quirks reproduced (fallback layout NaN x at default maxWidth; measureRunWidth drops MEASURE_FONT_PX). README carries the deviation and skip registers. Port after merge: 815 workspace tests + serial e2e 6/6 unchanged (0/60/31/0/18/1). Note: `cargo test --workspace` runs the e2e binary in parallel and it SIGSEGVs under GPU load from concurrent worker e2e runs; alone it passes. Steps 4–6 (renderer: instanced attributes, R32Float, fwidth, blending) wait on the blend branch.

## blend merged (2026-09-13 01:08)

Branch blend (8d4517d, 5 commits from 7ca38ca) merged into port at c1933b9. Blending enum + _getBlending table (src/materials/blending.rs, 7 unit tests vs the JS), blend state in RenderState/pipeline key, isOpaque gate on both DiffuseColor.w emissions (alphaHash not part of it at r186), Type::Mat2, instanced vertex attributes (Node::InstancedAttribute; instance_matrix switches to an interleaved attribute above 1024 instances and range() above 4096, keyed on max_uniform_buffer_binding_size like InstanceNode.js/RangeNode.js), per-draw resources from the per-draw NodeProgram, GPU buffers cached by node Rc identity. docs/nodes.md §9. Known divergence: attribute @location order on the attribute path (instance vec4s first). Workspace 835 tests serial, ladder 0/60/31/0/18/1 unchanged, dump_wgsl purely additive. Not done: alphaToCoverage multisample plumbing, colorWrite, SpriteNodeMaterial. Rung 13 and sdf-text steps 4–6 start from c1933b9. Note for everyone: run the e2e binary with --test-threads=1 only; multi-threaded it SIGSEGVs under GPU contention.

## rung 6 merged (2026-09-13 01:45)

rung6 (389256c, from 7ca38ca) merged into port at d33b0ce with four conflicts against blend (dump_wgsl, tsl.rs, wgsl.rs Type sizes, renderer node_buffer), all resolved additively; blend's tests updated for PerspectiveCamera.node, SetupContext.lights and the new attribute location order (position, instance vec4s, normal). Serial workspace 840 tests; ladder 0/60/31/0/0/18/1 with morphtargets at 0. OrthographicCamera still holds a bare Object3D; rung 7's shadow cameras are the trigger.

## rung 13 merged (2026-09-13 01:55)

rung13 (e67972f, from c1933b9) merged into port at ac00b8a with four conflicts against rung 6 (docs/nodes.md, node_material.rs, node.rs, tests/e2e/main.rs), resolved additively: the sprite position_view seam sits inside with_material_normal, BufferSource keeps Range{[f64;4]} and MorphInfluences, both e2e fns have full bodies. rung13's pre-rung-6 code moved to camera.node and ..SetupContext::default(). Serial workspace 843 tests; ladder 0/60/31/0/0/18/1/40 (eight examples). Usage reached the 10% weekly gate at 01:42: no further worker launches tonight; rung 7, rung 8 and sdf-text 4–6 are still running from 7ca38ca/c1933b9 and their merges will hit the same API moves (camera.node, SetupContext.lights, Range [f64;4], position_view keyed cache).

## rung 7 merged (2026-09-13 01:59)

rung7 (84316f8, from 7ca38ca) merged into port at d71c065 over blend + rung 6 + rung 13: 16 conflicting files. Rung 7's LightObject replaces rung 6's LightPayload (point_light.rs/ambient_light.rs removed); rung 6's sortLights-by-id kept and applied before render_shadows() so LightDesc indices, Light*/Shadow* uniforms and shadow-map keys agree; SetupContext.lights is Vec<LightDesc> next to morph; Phong loop is rung 7's (ambient_lights + direct_light) and reproduces morphtargets at 0 px; rung 7's two-variant Blending dropped for blend's table (Blending::No); Node::Loop takes a NodeRef count (rung 6's loop_statement wraps an int literal); shadow pass Renderables carry morph: None. Serial workspace 848 tests; ladder 0/60/31/0/0/18/1/7/40 (nine examples); dump_wgsl 72 sections, morph loop still `i < 2`. Gaps carried: morphing shadow casters, point-light cube shadows (rung 8 is building cube_depth_texture.rs), colorWrite/alphaToCoverage.

## sdf-text steps 4–6 merged (2026-09-13 02:20)

Branch sdf-text (1f32cef, 6 commits from c1933b9) merged into port at b10a0c2. Step 4: R32Float atlas with FLOAT32_FILTERABLE + linear filtering, fwidth/abs, position_node before the instance transform (deliberate r186 deviation, docs/nodes.md §10), sRGB/premultiplied handling; gate tests/sdf_text_glyph.rs (one glyph quad vs the atlas tile's own SDF resampled on the CPU: 0 wrong pixels; halo colour; UV orientation). Step 5: sdf-text/src/batched_text.rs (full BatchedText), examples/sdf_text_block.rs reproducing lib3's sdf-text-vector page (4 members, 58 glyph instances), RenderCamera trait so render() takes an OrthographicCamera; gate tests/sdf_text_block.rs (packing, two-run identity, whole 800x500 frame vs the atlas SDF: 0 wrong). Step 6: examples/d33_treemap_labels.rs (flare.json → d3-hierarchy treemap, 220 tiles, frameCamera ported verbatim, LABEL_PX fit, 30 labels / 423 glyph instances); gate tests/d33_treemap_labels.rs against tests/golden/d3_treemap_labels.json dumped from the page's own JS (all 220 rects, camera pose to 1e-9, all 30 labels' lines/values/anchors). Image number vs d33's d3_treemap.jpg: 2551/100000 px, printed not asserted — the diff is the page's white THREE.Line tile outlines (no line topology in the port) and MeshStandardNodeMaterial under Hemisphere+Directional lights (rung 8). Merge resolutions: RenderCamera exposes matrix_world()/layers() (PerspectiveCamera owns a Node since rung 6); rung 13's post-instancing position_node copy removed in favour of the pre-instancing one (galaxy unchanged at 40 px, its instance block is now live); BufferSource::Attribute next to Range [f64;4] and MorphInfluences; shadow_material maps Side::Double to itself. Serial workspace 857 tests; ladder 0/60/31/0/0/18/1/7/40; dump_wgsl 72 sections. Left out by the worker: step 5's browser grader (lib3 has no browser binary), BatchedText raycast/dispose, options.font/ready. **The d33 path now renders end to end**: d3 treemap → labels → SDF text on three-rs. What it still needs for a pixel match: LineBasicNodeMaterial/line topology (tile outlines) and rung 8's PBR + HemisphereLight.

## rung 8 merged (2026-09-13 02:30)

Branch rung8 (c7d6907) merged into port at 76a21b7. Rung 8 was cut before rung 7 landed and had grown its own light/shadow system (LightPayload, PointLightShadow, materials::LightKind, MAX_LIGHTS, a `shadows` parameter on `materials::setup`); the merge keeps rung 7's LightObject/LightShadow and folds rung 8 into it: `LightKind::Hemisphere` + `ground_color` on LightObject, `PointLight::new` carries `LightShadow::point()` with `update_point_matrices`, `Renderer::render_point_shadow` renders the six cube faces into `cube_shadow_targets`, `ShadowMap::{Planar, Cube}` in `LightDesc.shadow_map`, `phong::setup_light` shared by Phong and Physical, `Node::IfVar` beside HEAD's statement `Node::If`, `TextureKind::DepthCube` with comparison samplers for both depth kinds. Also carried over the rung 6/13/sdf-text API moves (camera.node, RenderCamera, position_node before instancing). One merge slip caught by the grader: the per-item UniformContext lost `tone_mapping_exposure` during de-duplication, which put lights_physical at 65.9% (floor far too bright) until restored. Serial e2e ladder 0/60/31/4/0/0/18/1/7/40, 10/10 green; workspace 858 passed, 0 failed; dump_wgsl runs (rung 8 blocks rebuilt on LightDesc/ShadowMap). Worktree rung8 removed, branch kept.

## Recap for the night (2026-09-13 02:30, usage gate reached)

Usage hit the 10% weekly gate at 02:04, so no workers were launched after that; everything running was finished, verified and merged. Where things stand:

- **port tip 76a21b7.** Ten e2e rungs green (1, 2, 3, 4, 5, 6, 7, 8, 9, 13), all verified by the director with serial reruns and the strips looked at. Workspace 858 tests, 0 failed. All worktrees but main/handoff/port/scouts removed; every branch kept.
- **What the port can do now:** node system generating both stages from TSL, Basic/Phong/Standard(Physical)/Sprite materials, Ambient/Point/Spot/Directional/Hemisphere lights, planar and cube shadow maps, morph targets, instancing, render targets, PassNode compositing, fog, tone mapping, sprites/galaxy, SDF text (sdf-text crate: atlas, BatchedText, d33 treemap labels).
- **d33 use case:** `examples/d33_treemap_labels.rs` renders the d3 treemap with labels end to end. Pixel match vs d33's d3_treemap.jpg is 2551/100000 (printed, not asserted). Remaining gaps: (1) Line primitives / LineBasicNodeMaterial for the white tile outlines (done, see lines merged); (2) switch the tiles to MeshStandardNodeMaterial under Hemisphere + Directional, which rung 8 now provides. (2) is a small example change plus a rerun of the d33 gate; (1) is a new rung-sized piece of work (line topology in the renderer + material).
- **Next rungs:** 10 (webgpu_loader_gltf: MeshStandard + KHR specular/ior fields already present), then 11 and 12 per handoff/scouts/rung11 and rung12 PLAN.md. Rung 8's progress note is at docs/rung8-progress.md on port.
- **Known nits on port:** phong.rs keeps rung 7's PointLightUniforms/direct_point_light (dead code, superseded by setup_light); docs/nodes.md carries both the rung 6 flat-shading key paragraph and the rung 8 normal_key paragraph in §7, which should be folded into one.

## lines merged (2026-09-13 10:25)

Branch lines (6d18f22, 5 commits from 76a21b7) fast-forwarded onto port. Hairline line primitives: `Line` (line strip) and `LineSegments` (line list) as Payload arms with the topology and strip index format taken from the object (`WebGPUUtils.getPrimitiveTopology` + `_getPrimitiveState`) into the pipeline descriptor and cache key; `MeshBasicNodeMaterial::line(color)` with a `LineBasicNodeMaterial` alias, no new MaterialKind: Three's real LineBasicNodeMaterial program was dumped off d33's d3_treemap.html (docs/lines/LineBasicNodeMaterial_27.*) and the port's WGSL is identical up to the §8 divergences already on the list. Lines cast shadows through the shared plumbing. tests/renderer_lines.rs (4 tests, 64x64 orthographic readback; corner pixels not asserted because of the diamond-exit rule). d33 treemap gained its 220 white tile outlines exactly as the page: image number 2551 -> 156 of 100000, now asserted under 250 in tests/d33_treemap_labels.rs; verified by the director (serial e2e rerun on the branch 10/10, ladder 0/60/31/4/0/0/18/1/7/40 unchanged, strip looked at: outlines sit on the page's, all residue is the slab's near silhouette, the unlit side wall of the Basic-material tiles). Workspace 862 passed, 0 failed, 4 ignored. Left out: LineLoop (WebGPURenderer errors on it), computeLineDistances/dashed, Points, mesh wireframe, linewidth. Next for d33: MeshStandard tiles under Hemisphere + Directional (rung 8 provides all three), which should take the remaining 156 px.
